import {
  Combination,
  combinationMapping,
  combinationStrength,
  PlayerItem,
  Rank,
  Suit,
  Card,
  GameState,
} from "./types";

export const getCombination = (holeCards: Card[], communityCards: Card[]) => {};
